import { AdvancedIfDirective } from './advanced-if.directive';

describe('AdvancedIfDirective', () => {
  it('should create an instance', () => {
    const directive = new AdvancedIfDirective();
    expect(directive).toBeTruthy();
  });
});
